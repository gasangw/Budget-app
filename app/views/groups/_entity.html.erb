<div class="container-fluid">
<div class="bills">
  <% entities.order('id DESC').each do |entity| %>
    <%= link_to group_entity_path(entity.group_id, entity) do %>
     <div class="mb-3">
      <div class="container-flud mx-2 px-2 bg-white border-bottom">
        <p class="pt-2"><%= entity.name %></p>
        <p class="pb-1 mb-0">Transactionº<%= entity.created_at.to_time.to_i %></p>
        <span class="fa fa-calendar"></span> <span class="mx-1"><%= entity.created_at %></span>
      </div>
      <div class="container-flud mx-2 px-2 bg-white border-bottom">
        <p class="py-2 m-0 d-flex justify-content-between">
        <strong>Amount:</strong>
        $<%= entity.amount %>
        </p>
      </div>
      </div>
      <% end %>
  <% end %>
</div>
</div>